services:
  - type: web
    name: district-noir-server
    runtime: node
    plan: free
    rootDir: .
    buildCommand: npm install && npm run build --workspace=server
    startCommand: npm run start --workspace=server
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
